const ProductList = ({MenuList}) => {
    return (
        <>
            {/* {MenuList.map(Menu=>(
                <h1>{Menu}</h1>
            ))} */}
            <h1>Hello</h1>
        </>
    );
}

export default ProductList;